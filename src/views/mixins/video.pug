mixin video(video)
    a(href=`/videos/${video.id}`)
        div.thumbnail
        div
            span=video.title
            div
                span #{video.owner.name} • 
                span #{video.meta.views} 회